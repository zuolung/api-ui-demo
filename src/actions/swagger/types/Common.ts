/** @type swagger(标注swagger生成的代码，请确认后修改) */
import { MyJson1Core3 } from '../baseTypes'

/**
 * 获取枚举数据
 * @url /api/lowcodecenter/common/enum
 * @method get
 * @introduce --
 */
export type IcommonEnum = {
  request: Record<string, any>

  response: MyJson1Core3
}
